<?xml version="1.0" encoding="utf-8"?>
<androidx.drawerlayout.widget.DrawerLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/drawerLayout"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/continental_black"
    android:keepScreenOn="true">

    <FrameLayout
        android:id="@+id/rootPlayer"
        android:layout_width="match_parent"
        android:layout_height="match_parent">

        <androidx.media3.ui.PlayerView
            android:id="@+id/playerView"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:background="@color/continental_black"
            app:controller_layout_id="@layout/view_player_controls"
            app:show_buffering="when_playing"
            app:surface_type="surface_view"
            app:use_controller="true"
            app:animation_enabled="true"
            app:resize_mode="fit" />

        <View
            android:id="@+id/gestureOverlay"
            android:layout_width="match_parent"
            android:layout_height="match_parent" />

        <TextView
            android:id="@+id/tvPerformanceDashboard"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="start|top"
            android:layout_margin="32dp"
            android:background="#B3000000"
            android:padding="@dimen/spacing_s"
            android:textColor="@color/continental_text_primary"
            android:textSize="11sp"
            android:fontFamily="monospace"
            android:visibility="gone" />

        <!-- Vertical Progress Bars for Gestures -->
        <ProgressBar
            android:id="@+id/pbVolume"
            style="?android:attr/progressBarStyleHorizontal"
            android:layout_width="200dp"
            android:layout_height="12dp"
            android:layout_gravity="start|center_vertical"
            android:layout_marginStart="-80dp"
            android:progressDrawable="@drawable/bg_progress_vertical"
            android:rotation="270"
            android:max="100"
            android:visibility="gone" />

        <ProgressBar
            android:id="@+id/pbBrightness"
            style="?android:attr/progressBarStyleHorizontal"
            android:layout_width="200dp"
            android:layout_height="12dp"
            android:layout_gravity="end|center_vertical"
            android:layout_marginEnd="-80dp"
            android:progressDrawable="@drawable/bg_progress_vertical"
            android:rotation="270"
            android:max="100"
            android:visibility="gone" />
            
        <TextView
            android:id="@+id/tvGestureIndicator"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="center"
            android:background="@drawable/bg_card"
            android:paddingStart="@dimen/spacing_l"
            android:paddingEnd="@dimen/spacing_l"
            android:paddingTop="@dimen/spacing_s"
            android:paddingBottom="@dimen/spacing_s"
            android:textColor="@color/continental_text_primary"
            android:textSize="18sp"
            android:fontFamily="sans-serif-medium"
            android:visibility="gone" />

        <LinearLayout
            android:id="@+id/llErrorOverlay"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:layout_gravity="center"
            android:background="@color/continental_black"
            android:gravity="center"
            android:orientation="vertical"
            android:padding="@dimen/spacing_xl"
            android:visibility="gone">

            <TextView
                android:id="@+id/tvErrorTitle"
                style="@style/Continental.Text.Title"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:textSize="18sp"
                android:text="@string/error_generic_playback_title" />

            <TextView
                android:id="@+id/tvErrorBody"
                style="@style/Continental.Text.Body"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginTop="@dimen/spacing_s"
                android:gravity="center" />

            <LinearLayout
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginTop="@dimen/spacing_l"
                android:orientation="horizontal">

                <com.google.android.material.button.MaterialButton
                    android:id="@+id/btnErrorRetry"
                    style="@style/Widget.Material3.Button.OutlinedButton"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:text="@string/action_retry" />

                <com.google.android.material.button.MaterialButton
                    android:id="@+id/btnErrorSkip"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:layout_marginStart="@dimen/spacing_m"
                    android:backgroundTint="@color/continental_gold"
                    android:textColor="@color/continental_black"
                    android:text="@string/action_skip_next" />
            </LinearLayout>
        </LinearLayout>

        <!-- Equalizer Overlay (Hidden by default) -->
        <FrameLayout
            android:id="@+id/equalizerContainer"
            android:layout_width="320dp"
            android:layout_height="wrap_content"
            android:layout_gravity="center"
            android:background="@drawable/bg_card"
            android:elevation="8dp"
            android:visibility="gone">
            
            <LinearLayout
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:orientation="vertical"
                android:padding="@dimen/spacing_m">
                
                <TextView
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:text="Audio Equalizer"
                    android:textColor="@color/continental_gold"
                    android:textSize="18sp"
                    android:fontFamily="sans-serif-medium"
                    android:layout_marginBottom="@dimen/spacing_s" />
                    
                <LinearLayout
                    android:id="@+id/equalizerBands"
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:orientation="horizontal"
                    android:weightSum="5">
                    <!-- Bands will be added dynamically -->
                </LinearLayout>
                
                <com.google.android.material.button.MaterialButton
                    android:id="@+id/btnCloseEqualizer"
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:layout_marginTop="@dimen/spacing_m"
                    style="@style/Widget.Material3.Button.TextButton"
                    android:textColor="@color/continental_gold"
                    android:text="Close" />
            </LinearLayout>
        </FrameLayout>
    </FrameLayout>

    <!-- Playlist Drawer -->
    <LinearLayout
        android:id="@+id/playlistDrawer"
        android:layout_width="300dp"
        android:layout_height="match_parent"
        android:layout_gravity="end"
        android:background="#1C1C1C"
        android:orientation="vertical"
        android:padding="@dimen/spacing_m">

        <TextView
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="Playlist"
            android:textColor="@color/continental_gold"
            android:textSize="20sp"
            android:fontFamily="sans-serif-medium"
            android:paddingBottom="@dimen/spacing_s" />

        <androidx.recyclerview.widget.RecyclerView
            android:id="@+id/rvPlaylist"
            android:layout_width="match_parent"
            android:layout_height="0dp"
            android:layout_weight="1"
            android:clipToPadding="false" />
            
        <com.google.android.material.button.MaterialButton
            android:id="@+id/btnAddFiles"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="@dimen/spacing_s"
            android:backgroundTint="@color/continental_surface_elevated"
            android:textColor="@color/continental_text_primary"
            android:text="Add Files" />
    </LinearLayout>

</androidx.drawerlayout.widget.DrawerLayout>
